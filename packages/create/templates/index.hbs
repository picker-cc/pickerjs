{{#if isTs }}import { bootstrap } from '@pickerjs/core';{{else}}const { bootstrap } = require('@pickerjs/core');{{/if}}
{{#if isTs }}import { config } from './picker-config';{{else}}const { config } = require('./picker-config');{{/if}}

bootstrap(config).catch(err => {
    // tslint:disable-next-line:no-console
    console.log(err);
});
